//Array methods

var students = ['Achi', 'Oke', 'Sussy'];

while (students.length > 0) {
  let student = students.shift();
  console.log(`Hello, ${student}`);

}

var studentNames = ['Abena', 'Koffi', 'Mami'];

while (studentNames.length > 0) {
  let student = studentNames.pop();
  console.log(`Hello, ${student}`);

}