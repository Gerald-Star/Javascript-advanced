// Using three Pillars of JS
/*
Types / Coercion, Scope/Closure and this prototype
*/

// Instruction

/*
Modification to use the Javascript types Coercion, this

1. In the printFavorite() function make sure there is no accidental tye conversion (ie, from to string)

! Hint- use the String(...) to coerce something to a string type

2. Move the addFavoriteBook(...) and printFavoriteBooks() functions into the BookShelf class as methods

Modify the use of this keyword to access the favoriteBooks array.

 ! Hint: Class methods don't use the function keyword just their name.

3. Fill out the definition of the loadBooks(..) function, which should 
receive an instance of the BookShelf class that 

*/